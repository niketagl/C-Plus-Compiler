first install lex:
  sudo apt-get install flex

compile one.l using lex program :
  lex one.l
  
then compile test file along with generated code:
  g++ -o LEX_ME test.cpp lex.yy.c
  
run and provide input in stdin :
  ./LEX_ME < sample_input.txt
